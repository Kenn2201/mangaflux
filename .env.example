# Browser-visible configuration. This URL is public by design.
# Never put secrets in NEXT_PUBLIC_* variables.
NEXT_PUBLIC_API_URL=http://localhost:4000

# Vercel / Next server-only upstream URL.
MANGAFLUX_API_URL=http://localhost:4000

# Render / API
PORT=4000
WEB_ORIGIN=http://localhost:3000
MANGADEX_BASE_URL=https://api.mangadex.org
ENABLE_BROWSER_SOURCE=false

# Server-only secrets. Keep values in Render/Vercel environment settings.
DATABASE_URL=
AUTH_SECRET=
